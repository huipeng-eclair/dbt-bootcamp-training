version: 2

sources:
  - name: globalmart
    database: raw
    schema: globalmart
    tables:
      - name: customer
      - name: orders
        columns:
          - name: orderid
          # test in the source database
          # command: dbt test -s source:globalmar
            tests:
              - unique
              - not_null
      - name: product
        # columns:
        #   freshness:
        #     warn_after:
        #       count: 12
        #       period: hour
        #     error_after:
        #       count: 24
        #       period: hour
        #   loaded_at_field: loaded_at_field 